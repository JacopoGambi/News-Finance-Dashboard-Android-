package com.example.newsfinance.domain.repository

/**
 * Interfaccia repository per le notizie remote.
 */
interface NewsRepository

/**
 * Interfaccia repository per le crypto remote.
 */
interface CryptoRepository

/**
 * Interfaccia repository per i preferiti locali (Room).
 */
interface FavoritesRepository
